{% extends 'base.html.twig' %}
{% block seodescription %}<meta name="description" content="{{ page.seoDescription }}"> <!-- Meta Description --> {% endblock %}
{% set parameters = var_parameters.parameters %}
{% block title %}{{parameters.company}} - {{ page.action.name }}{% endblock %}
{% set hasContactForm = true %}

{% block body %}
    <main class="page">
        <h1 class="max-width">{{ page.name}}</h1>
        {% for pageContainer in  page.pageContainers %}
            {% set mode = 'view' %}
            {% include "/page/container.html.twig" %}
        {% endfor %}
    </main>
{% endblock %}
