{% extends 'base.html.twig' %}

{% block title %}Une pause{% endblock %}

{% block body %}
<section class="grid-2-4-2">
    <img src="/images/{{article.images[0].filename}}" alt="{{article.images[0].title}}">
    <div>
        <h1>{{ article.title }}</h1>
        <div>
            {{ article.content| raw }}
        </div>
    </div>
    <img src="/images/{{article.images[1].filename}}" alt="{{article.images[0].title}}">
</section>

{% endblock %}
